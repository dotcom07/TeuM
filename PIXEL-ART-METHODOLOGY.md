# TeuM 코드형 픽셀아트 방법론

## 목표

TeuM의 도트맵은 단순한 팔레트 교체가 아니라, 테마 이름을 가리지 않아도
실루엣·재질·소품 관계만으로 주제를 알아볼 수 있어야 한다.

## 참고한 원칙

- Lospec의 튜토리얼·팔레트 자료: 제한 팔레트, 읽히는 픽셀 클러스터, 불필요한
  단독 픽셀 억제
  <https://lospec.com/pixel-art-tutorials/>
- Kenney CC0 Animal Pack: 동물마다 귀·주둥이·몸통 비율을 다르게 잡아 작은
  크기에서도 종을 구분하는 실루엣 참고
  <https://kenney.nl/assets/animal-pack>
- MDN SVG `shape-rendering`: 정수 격자와 `crispEdges`로 안티앨리어싱 없는 확대
  결과 유지
  <https://developer.mozilla.org/en-US/docs/Web/SVG/Reference/Attribute/shape-rendering>

참고 자료는 복사 원본이 아니라 실루엣과 픽셀 클러스터를 분석하는 용도로만
사용한다. 실제 아트는 TeuM의 64×40 격자와 고정 팔레트에 맞춰 다시 설계한다.

## 테마별 작업 순서

1. 한 문장 콘셉트를 먼저 쓴다.
2. 테마 전용 대표 형태 4개와 재질 2개를 정한다.
3. 벽지는 짧은 타일 반복 대신 비대칭 구역·큰 패턴·작은 보조 패턴을 섞는다.
4. 14개 아이템은 각각 다른 외곽 실루엣을 사용한다.
5. 펫은 색보다 귀, 얼굴, 몸통, 꼬리, 자세 중 최소 2개를 바꾼다.
6. 장면을 합성해 아이템 경계색과 바로 뒤 픽셀의 색이 같은 위치를 검사한다.
7. 단품판과 전체 장면을 렌더링하고 사람 눈으로 2회 검토한다.
8. 사용자 피드백을 반영해 확정한 뒤 다음 테마로 이동한다.

## 코드 구현 규칙

### 정수 격자

- 모든 아트는 슬롯 원본 해상도에서 문자열 행으로 작성한다.
- 좌표, 너비, 높이는 정수만 사용한다.
- 확대 시 보간하지 않고 각 아트 픽셀을 같은 크기의 사각형으로 그린다.

### 비대칭 배경

`paintRows(width, height, fill, stamps)`로 큰 바탕 위에 서로 다른 크기의
스탬프를 배치한다. 같은 8×8 타일을 화면 전체에 반복하는 방식은 쓰지 않는다.

```ts
const wallpaper = paintRows(64, 31, "E", [
  { x: 0, y: 0, rows: ["K".repeat(64)] },
  { x: 23, y: 4, rows: CAT_SILHOUETTE },
  { x: 4, y: 18, rows: YARN_TRAIL }
]);
```

### 색 분리

- 벽지와 바닥에는 중간 명도의 넓은 면을 사용한다.
- 소품은 배경에 없는 어두운 외곽선 또는 밝은 림을 사용한다.
- 펫이 책상과 겹치는 위치는 전체 외곽선을 한 색으로 두지 않고, 실제로
  겹치는 픽셀만 대비되는 포인트색으로 바꾼다.
- 단품 카드에서 잘 보이는지만 확인하지 않는다. 반드시 최종 장면 합성 결과를
  기준으로 검사한다.

### 재색칠 복제 금지

아래 중 하나라도 같으면 새 펫으로 인정하지 않는다.

- 투명/불투명 마스크가 기존 펫과 완전히 동일함
- 귀·얼굴·몸통·꼬리의 외곽 좌표가 모두 동일함
- 아이템 14종의 슬롯별 형태가 이전 테마와 동일하고 색 토큰만 다름

## 자동 검증

```bash
npm run art:audit
npm run art:audit -- --theme cat
npm run art:review -- --theme cat
```

`art:audit`은 다음을 출력한다.

- 테마별 장면 경계색 충돌 수
- 벽지의 짧은 가로·세로 반복 주기
- 같은 불투명 마스크를 공유하는 펫 묶음

테마 확정 조건은 경계색 충돌 0, 짧은 벽지 반복 없음, 육안 검토 통과다.
