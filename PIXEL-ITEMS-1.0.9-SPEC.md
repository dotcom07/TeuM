# 틈새움 1.0.9 픽셀 수집 시스템

## 1. 기준

1. 1.0.9의 원화 기준은 선물상자 도입 전인 1.0.6이다.
2. 장면은 64×40 아트 그리드, 아이템은 기존 슬롯 크기를 유지한다.
3. 기본 테마의 26개 도트맵과 클래식 검정냥이·기본 강아지 실루엣은 1.0.6 앵커 해시로 고정한다.
4. 기본 검정냥이와 강아지는 각각 6×7 실루엣을 고정한다.
5. 신규 고양이·강아지는 고정 실루엣에서 몸색, 얼굴 무늬, 1~2픽셀 소품만 바꾼다.
6. 신규 종은 형태를 확실히 구분할 수 있는 토끼·곰·유령·파랑새만 사용한다.
7. 가구·윤곽·소품은 기존 18색 공용 팔레트를 사용하고, 벽지의 가장 넓은 면만 테마 전용 배경색을 사용한다.
8. 보간·안티앨리어싱을 사용하지 않는다.

## 2. 테마

1.0.7에서 가져오는 것은 아래 주제뿐이며 원화와 구현 코드는 재사용하지 않는다.

| 묶음 | 테마 |
| --- | --- |
| 기존 고정 | 기본, 고양이와 생쥐, 여름, 가을 |
| 계절 | 봄, 겨울, 크리스마스, 장마 |
| 자연·여행 | 아쿠아리움, 해저, 하늘, 캠핑, 온실 |
| 동물·이야기 | 삼색고양이, 동물원, 꿈, 판타지 |
| 공간·미래 | SF, 우주, 학교, 도서관, 카페, 베이커리 |
| 취향·문화 | 음악실, 오락실, 한옥, 밤도시 |

신규 테마도 1.0.6과 동일하게 14개 슬롯으로 구성한다.

- 벽지, 바닥, 창문, 책상, 모니터
- 컵, 식물/장식, 조명, 데스크 매트
- 선반 장식, 액자, 시계
- 펫, 바닥 장식

### 테마 배경 팔레트

- 기본을 제외한 26개 테마는 서로 다른 전용 주 배경색과 전용 소문자 토큰을 하나씩 가진다.
- 테마 전용색은 벽지의 가장 넓은 불투명 면에만 적용한다. 아이템 실루엣과 세부 무늬는 바꾸지 않는다.
- 동일 HEX를 재사용하지 않으며, 모든 테마 쌍의 CIE Lab ΔE76 색상 거리는 15 이상이어야 한다.
- 비슷한 계열이 필요한 경우에도 명도·채도·색상 중 최소 하나를 벌려 전체 장면에서 바로 구분되게 한다.
- 검정과 흰색 대비칩이 포함된 팔레트 비교판을 생성해 텍스트와 아이템 외곽 가독성을 함께 확인한다.

## 3. 보상 규칙

- 특별 날짜, 계절 한정, 이스터에그 숫자, 특정 시각 획득 조건은 사용하지 않는다.
- 누적 완료 5회마다 열 수 있는 선물상자 1개를 지급한다.
- 선물상자는 아직 없는 일반 아이템 중 하나를 무작위로 지급한다.
- 누적 완료 10회마다 선택 포인트 1P를 지급한다.
- 도감에서 이름을 확인한 뒤 5P를 사용하면 원하는 미소장 아이템 하나를 확정 획득한다.
- 확정 획득 1회에는 총 50번의 완료가 필요해 랜덤 획득보다 드물고 특별해야 한다.
- 기본 지급 아이템은 선물상자와 포인트 구매 후보에서 제외한다.

## 4. UX

### 홈

- 다음 선물상자까지 `3 / 5`처럼 진행도를 표시한다.
- 보유 선택 포인트를 항상 같은 위치에 표시한다.
- 미개봉 상자가 있으면 홈 진입 시 선물상자 모달을 한 번에 하나씩 연다.

### 선물상자

1. 닫힌 상자와 `열기` 버튼을 보여 준다.
2. 탭하면 도트 반짝임과 함께 뚜껑이 열린다.
3. 아이템 이름과 원화를 보여 준다.
4. 남은 상자가 있으면 `다음 상자`, 없으면 `도감에서 보기`를 제공한다.
5. 의성어 문자열은 화면에 표시하지 않는다.

### 도감

- 기본 및 26개 테마를 접을 수 있는 섹션으로 묶는다.
- 미소장 상태에서도 이름은 그대로 보여 준다.
- 미소장 원화만 `??`로 가린다.
- 미소장 카드를 누르면 보유 포인트와 5P 비용을 보여 주는 확인창을 연다.
- 포인트가 부족하면 다음 1P까지 남은 완료 횟수를 안내한다.

## 5. 저장과 마이그레이션

- 데스크 저장 형식을 v2로 올리고 `ownedItemIds`, `openedGiftCount`, `spentChoicePoints`를 저장한다.
- 1.0.6의 v1 저장을 처음 읽을 때 기존 누적 횟수로 이미 열렸던 아이템을 모두 보존한다.
- 업데이트 직후 과도한 상자 모달이 쌓이지 않도록 과거 구간의 상자는 열린 것으로 처리한다.
- 과거 누적 횟수로 적립된 선택 포인트는 그대로 사용할 수 있게 한다.
- 기존 배치에 없는 아이템 ID가 있으면 제거하고 기본 배치로 안전하게 복구한다.

## 6. 출시 조건

- 1.0.6 앵커 해시 테스트가 통과해야 한다.
- 중간검수 기준은 Git 태그 `art-anchor-1.0.9-midreview`로 복원할 수 있어야 한다.
- 모든 신규 아이템은 슬롯 크기, 행 너비, 팔레트, 외곽 대비 검사를 통과해야 한다.
- `art:audit-palettes`에서 26개 전용색 중복 0개, 최소 ΔE76 15 이상을 통과해야 한다.
- 모든 펫은 허용한 실루엣 중 하나와 일치해야 한다.
- 선물상자 중복 지급, 포인트 과소·과다 차감, v1 마이그레이션 테스트가 통과해야 한다.
- `art:review`로 전체 카탈로그·전체 테마·펫·테마 팔레트 비교판을 생성한다.
- 사용자에게 비교판과 실제 UI를 보여 주고 명시적으로 승인받기 전에는 Play Console에 올리지 않는다.
